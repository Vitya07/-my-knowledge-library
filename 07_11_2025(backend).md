# Собрал мысли по бэку 

# 🏗️ Архитектура Go-бэкенд проекта

Этот документ описывает рекомендуемую структуру и принципы организации кода для backend-приложений на языке Go.  
Цель — обеспечить читаемость, тестируемость, поддерживаемость и масштабируемость.

---

## 🔑 Основные принципы

1. **Разделение ответственности (Separation of Concerns)**  
   Каждый компонент решает одну задачу и не знает о внутреннем устройстве других.

2. **Независимость от фреймворков**  
   Бизнес-логика не зависит от HTTP-фреймворка (Fiber, Gin, Echo и т.д.).

3. **Тестируемость**  
   Любой слой можно протестировать изолированно без запуска сервера.

4. **Безопасность через изоляцию**  
   Внутренние компоненты недоступны извне благодаря папке `internal/`.

5. **Конфигурируемость**  
   Настройки вынесены из кода — легко менять поведение под разные окружения.

---

## 📂 Структура проекта
<img width="363" height="630" alt="image" src="https://github.com/user-attachments/assets/5050e198-6a73-4bd0-9fcf-a581efac1b8a" />


> 📌 **`internal/`** — особая папка в Go:  
> любой код внутри неё **нельзя импортировать из других проектов**.  
> Это гарантирует, что внутренняя реализация остаётся скрытой.

