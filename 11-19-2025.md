 Стек вызовов в Go:
---

## 🔹 Что такое стек вызовов?

**Стек вызовов** (call stack) — это структура данных в памяти программы, которая отслеживает **активные вызовы функций**.

Каждый раз, когда одна функция вызывает другую, в стеке создаётся **новый фрейм** (frame), содержащий:
- имя функции,
- её локальные переменные,
- переданные параметры,
- адрес возврата (куда вернуться после завершения).

Когда функция завершается, её фрейм **удаляется** из стека, и управление возвращается к предыдущей функции.

В Go стек работает по принципу **LIFO** (Last In — First Out): последняя вызванная функция завершается первой.
