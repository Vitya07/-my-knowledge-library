# Массивы vs Связанные списки

При выборе структуры данных для хранения последовательности элементов часто возникает вопрос: использовать **массив** или **связанный список**? Ниже — ключевые различия, плюсы, минусы и рекомендации по применению.

---

## 📦 Массив (Array)

### ✅ Плюсы
- **Быстрый произвольный доступ**: любой элемент можно получить за **O(1)** по индексу.
- **Кэш-дружелюбность**: элементы расположены в памяти последовательно, что ускоряет чтение.
- **Минимальные накладные расходы**: не хранит дополнительные указатели.

### ❌ Минусы
- **Фиксированный размер** (в классическом виде): при переполнении требуется выделение новой памяти и копирование всех элементов (**O(n)**).
- **Неэффективные вставки/удаления** в середине или начале: нужно сдвигать элементы (**O(n)**).
- **Требует непрерывного блока памяти**, что может быть проблемой при больших размерах.

### 🛠 Когда использовать
- Когда нужно часто **читать элементы по индексу**.
- Размер данных **известен заранее** или почти не меняется.
- Приоритет — **высокая скорость доступа** и **простота**.

---

## 🔗 Связанный список (Linked List)

### ✅ Плюсы
- **Эффективные вставки и удаления** (в начале или после известного узла): **O(1)**.
- **Не требует непрерывной памяти** — элементы могут быть разбросаны.
- **Динамический размер**: легко расширяется без копирования.

### ❌ Минусы
- **Нет произвольного доступа**: чтобы добраться до i-го элемента, нужно пройти все предыдущие (**O(n)**).
- **Больше памяти на каждый элемент** (хранится указатель/ссылка на следующий узел).


### 🛠 Когда использовать
- Когда часто выполняются **вставки/удаления** в начало или середину списка.
- Размер данных **непредсказуем** и часто меняется.
- Последовательный обход (например, очередь или стек на базе списка).

---

## 💡 Итог
> — Нужен **быстрый доступ по индексу** → **массив**.  
> — Нужны **частые вставки/удаления** → **связанный список**.
