# 📚 База знаний бэкенд-разработчика (Go)

Краткие определения, примеры и пояснения для самых важных понятий в backend-разработке.

---

## 🔌 HTTP (HyperText Transfer Protocol)

**Что это?**  
Протокол передачи данных в интернете. По нему браузер общается с сервером.

**Основные методы (запросы):**

| Метод   | Назначение                              | Идемпотентность | Безопасность |
|--------|----------------------------------------|------------------|--------------|
| `GET`  | Получить данные                        | ✅ Да            | ✅ Да        |
| `POST` | Создать ресурс                         | ❌ Нет           | ❌ Нет       |
| `PUT`  | Полностью обновить ресурс              | ✅ Да            | ❌ Нет       |
| `PATCH`| Частично обновить ресурс               | ❌ Нет           | ❌ Нет       |
| `DELETE`| Удалить ресурс                        | ✅ Да            | ❌ Нет       |

> 💡 **Идемпотентность** — повторный запрос даёт тот же результат.  
> **Безопасность** — запрос не изменяет состояние сервера.

---

## 🌐 REST API

**Что это?**  
Стиль архитектуры для построения веб-API, основанный на HTTP и ресурсах.

**Принципы REST:**
- Всё — **ресурс** (пользователь, задача, заказ).
- Ресурсы идентифицируются **URL**: `/users`, `/users/1`.
- Операции — через **HTTP-методы** (`GET`, `POST`, `PUT`, `DELETE`).
- Сервер **без состояния** (stateless) — каждый запрос содержит всё нужное.
- Ответы в **стандартном формате** (обычно JSON).

**Пример:**
```http
GET /users        → получить список
POST /users       → создать пользователя
GET /users/1      → получить пользователя по ID
DELETE /users/1   → удалить пользователя

<img width="1073" height="161" alt="image" src="https://github.com/user-attachments/assets/d7cd3c91-234f-4102-8a88-d3e534c3db74" />


```go
type User struct {
    Name string `json:"name"`
    Age  int    `json:"age"`
}

// В объект → JSON
user := User{Name: "Виктор", Age: 18}
jsonData, _ := json.Marshal(user)
// Результат: {"name":"Виктор","age":18}

// Из JSON → в объект
var u User
json.Unmarshal(jsonData, &u)
```

<img width="959" height="548" alt="image" src="https://github.com/user-attachments/assets/7c8bf3bd-c160-402c-b499-dbe850bbcdf6" />


<img width="608" height="298" alt="image" src="https://github.com/user-attachments/assets/f48e1828-17a9-4d24-a673-ea84877208f6" />

<img width="1049" height="594" alt="image" src="https://github.com/user-attachments/assets/a9627d4e-c84b-45fa-b4b8-ca896e6eb571" />

<img width="1064" height="485" alt="image" src="https://github.com/user-attachments/assets/dd454cd6-da62-45f3-974f-b5f23fe64b28" />


